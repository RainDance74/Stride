{
  "ConnectionStrings": {
    // Put a variable with connection string in your system (default is for user, if not, change DI set up in Infrastructure)
    "EnvironmentKey": "POSTGRES_CONSTR"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
